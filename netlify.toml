# Netlify auto-detects Next.js and applies the OpenNext adapter (SSR, Server Actions, App Router).
# See: https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/overview/

[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
